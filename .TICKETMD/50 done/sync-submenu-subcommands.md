sync submenu/subcommands

after s (sn or snc):

if reminder list is connected:

“import from $LIST?” Y/n

if reminder list is not connected:

give list of reminder lists and new. overwrite tickets, make list vertical, not a single line

after identifying a list, go to reminder list is connected








---
id: 28
---
