all ticket folder live in .TICKETMD by default and in this project so they can be committed and ticket history stays with code

Same restructuring step already captured in the "initial git commit,
push..." ticket's resolved plan - pulled out here as its own standalone
piece. Repository/setup! default to a hidden `.TICKETMD/` subfolder
(status folders + app-managed files like .reminders-list, .TRASH), and
this project's existing folders get physically moved there too.


---
id: fad4d0
ready: true
---



---
id: fad4d0
---
