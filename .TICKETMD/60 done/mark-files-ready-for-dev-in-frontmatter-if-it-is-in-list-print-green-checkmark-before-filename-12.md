mark files "ready for dev" in frontmatter if it is. in list, print green checkmark before filename

First real YAML frontmatter in the format - scoped to just this one
boolean flag for now (not the rest of the original spec's fields).
Whoever is implementing (agent or human) marks a ticket ready when they
judge it to be, by setting the flag in frontmatter. In the board/list,
show a green checkmark before the title for tickets marked ready.









---
id: 12
---
