demo data: just a few typical development tasks in each category

Hidden CLI subcommand `tmd demo` (not an interactive keypress - `d` is
already taken by delete, and this is meant to be secret/undocumented
anyway). Not listed in `tmd`'s usage/help text.

Toggle behavior:
- 1st run: stashes the current real tickets somewhere safe, replaces the
  active folders with demo data (a handful of typical dev tasks spread
  across every stage).
- 2nd run: deletes the demo data, restores the real tickets from the
  stash.








---
id: 29
ready: true
---
