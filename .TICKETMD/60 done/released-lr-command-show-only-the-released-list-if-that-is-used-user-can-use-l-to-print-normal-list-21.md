released lr command: show ONLY the RELEASED list if that is used - user can use l to print normal list

Plain `l` after the released-only exclusive view goes back to the normal
default board (released collapsed to a count). Numbering stays consistent
across the whole ticket set regardless of which view is showing, so a
ticket can always be identified the same way from a complete cross-folder
list - not per-view numbering.











---
ready: true
id: 21
---
