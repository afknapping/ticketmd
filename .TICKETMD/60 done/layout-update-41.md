layout update

below DONE: empty line, then full width ========== divider, then empty line, then list of commands, then empty line, then cursor

output / feedback happens where the cursor is, not above it

Done: added a full-width `=` divider below the board, moved the
command list below it, and moved @message to the very last line of
the frame (where the cursor sits) instead of above the command list.
Verified via pty test with a real terminal width.



---
id: 41
ready: true
---
